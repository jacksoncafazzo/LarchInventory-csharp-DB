
here it is dawg:
@foreach (var item in @Model)
{
  <ul>
    <li><a href="item/@item.GetId()">@item.GetName()</a></li>
    <li>Category: @item.GetCategory()</li>
    <li>Description: @item.GetDescription()</li>
    <li>Amount per item: @item.GetAmount()</li>
    <li>Price per item: $@item.GetPrice()</li>
    <form method="post" action="/delete_item/@item.GetId()">
    <li><button type="submit">Delete @item.GetName()</button></li>
    </form>
  </ul>
}
<h2>Add a new item here:</h2>
@Html.Partial("add_item_form.cshtml")
<h3> !!! Warning ! This deletes ALL items !!!
<a href="/delete_all">Delete All Items</a>
